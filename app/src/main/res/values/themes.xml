<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NotesAppWithRoomDB" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/pink</item>
        <item name="colorPrimaryVariant">@color/pink</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/pink</item>
        <item name="colorSecondaryVariant">@color/pink</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="Theme.NotesAppWithRoomDB" parent="Base.Theme.NotesAppWithRoomDB" />

    <!-- Add Splash Screen theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color to match your app's primary color -->
        <item name="windowSplashScreenBackground">@color/pink</item>

        <!-- Use the splash icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>

        <!-- Set the theme used after splash screen -->
        <item name="postSplashScreenTheme">@style/Base.Theme.NotesAppWithRoomDB</item>

        <!-- Status bar color for splash screen -->
        <item name="android:statusBarColor">@color/pink</item>
    </style>
</resources>